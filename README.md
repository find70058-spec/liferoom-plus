# liferoom-plus

Cloudflare Pages static information site for `plus.liferoom-j.com`.

## Build

```powershell
npm run build
```

## Deploy

```powershell
npx wrangler pages deploy dist --project-name liferoom-plus --branch main
```
